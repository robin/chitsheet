--- 
zsh_array: |-
  # Standard array
  	array=(~/.zshenv, ~/.zshrc, ~/.zlogout)
  	print ${array:t}
  
  # Associative array
  	typeset -A ass_arr
  	ass_arr=(one 1 two 2 three 3 four 4)
  	print ${(k)ass_arr}
  	print ${(v)ass_arr}
  	print $ass_arr[one]
